name: Direct SSH VPN (TCP Raw Fix)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Network Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y wireguard resolvconf ssh curl

      - name: Setup WireGuard Config
        run: |
          cat <<EOF | sudo tee /etc/wireguard/wg0.conf
          [Interface]
          PrivateKey = ${{ secrets.SERVEO_WG_PRIVATE_KEY }}
          Address = ${{ secrets.SERVEO_WG_ADDRESS || '10.1.1.2/32' }}

          [Peer]
          PublicKey = 7vL9mU/n5W/2X5l5p6yF9f+9L6w4J4f5Z6u7i8o9p0A=
          Endpoint = wireguard.serveo.net:51820
          AllowedIPs = 10.0.0.0/8
          PersistentKeepalive = 25
          EOF

      - name: Start WireGuard
        run: sudo wg-quick up wg0 || true

      - name: Create SSH User (Masoud)
        run: |
          # ساخت یوزر مسعود
          USER_PASS="13691369"
          ENCRYPTED_PASS=$(perl -e 'print crypt($ARGV[0], "password")' $USER_PASS)
          sudo useradd -m -p "$ENCRYPTED_PASS" -s /bin/bash masoud
          echo "User 'masoud' created."

      - name: Configure SSH Server
        run: |
          # تنظیمات نهایی برای پایداری تونل
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/g' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/g' /etc/ssh/sshd_config
          echo "AllowTcpForwarding yes" | sudo tee -a /etc/ssh/sshd_config
          echo "TCPKeepAlive yes" | sudo tee -a /etc/ssh/sshd_config
          sudo systemctl restart ssh

      - name: Establish Raw TCP Tunnel
        run: |
          echo "--------------------------------------------------------"
          echo "Connecting to Serveo (TCP Mode)..."
          echo "--------------------------------------------------------"
          
          # استفاده از پورت 0 برای اینکه Serveo خودش یک پورت تصادفی به ما بدهد
          # این پورت تداخل پروتکل ندارد و دیتای SSH را سالم منتقل می‌کند
          sudo ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=60 \
            -R 0:localhost:22 serveo.net 2>&1 &
          
          sleep 15
          echo "Check the logs above for 'Forwarding TCP traffic from serveo.net:XXXXX'"

      - name: Display Info
        run: |
          echo "===================================================="
          echo "IMPORTANT: In Netmod, use 'serveo.net' as Host"
          echo "and use the Port number given in the previous step."
          echo "User: masoud | Pass: 13691369"
          echo "===================================================="

      - name: Keep Server Alive
        run: |
          for i in {1..72}; do
            echo "Heartbeat $i: VPN Active..."
            sleep 300
          done
